package com.example.ioc.springioc;import org.springframework.beans.BeansException;import org.springframework.context.ApplicationContext;import org.springframework.context.ApplicationContextAware;import org.springframework.stereotype.Component;/** * 예제가 많은 코드인데 * * @Componenet로 등록된 Bean으로 관리되는 클래스에 접근할 ApplicationContextProvider라는 * ApplicationContextAware을 상속받아옴. */@Componentpublic class ApplicationContextProvider implements ApplicationContextAware {    private static ApplicationContext context;    // 1. ApplicationContextProvider를 생성시 setApplicationContext에서 applicationContext를 주입할 때 set으로    // 할당.    @Override    public void setApplicationContext(ApplicationContext applicationContext) throws BeansException {        context = applicationContext;    }    // 2. 할당 된  context를 받아다 쓰기만 하면 됨.    public static ApplicationContext getContext() {        return context;    }}