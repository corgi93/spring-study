package com.example.ioc.springioc;import org.springframework.beans.factory.annotation.Qualifier;import org.springframework.context.annotation.Bean;import org.springframework.context.annotation.Configuration;import org.springframework.stereotype.Component;import java.util.Base64;@Componentpublic class Encoder {    private IEncoder iEncoder;    /**     * 부모클래스인 Encoder도 @Component로 Bean으로 관리하게 되면 어떻게 매칭할 지 몰라서 에러.     * 이럴땐 @Qualifier로 이름으로 지정. @Component("baseEncoder")이렇게 지정 안하면 Base64Encoder를 첫글자를 소문자로     * 카멜케이스로 기본 생성.     */    public Encoder(@Qualifier("base64Encoder") IEncoder iEncoder) {        this.iEncoder = iEncoder;    }    // setMethod.    public void setiEncoder(IEncoder iEncoder) {        this.iEncoder = iEncoder;    }    public String encode(String message) {        return iEncoder.encode(message);    }}/** * Configuration은 하나의 클래스에서 여러 Bean을 생성할 거임. * * encoder라는 Bean이 2개 생성됬으므로 Naming으로 구별해준다. */@Configurationclass AppConfig{    @Bean("base64Encode")    public Encoder encoder(Base64Encoder base64Encoder){        return new Encoder(base64Encoder);    }    @Bean("urlEncode")    public Encoder encoder(UrlEncoder urlEncoder){        return new Encoder(urlEncoder);    }}