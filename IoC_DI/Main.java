public class Main {    public static void main(String args[]) {        String url = "www.naver.com/books/it?page=10&size=20";        /**         * before         *///        Encoder encoder = new Encoder();//        String res = encoder.encode(url);//        System.out.println(res);        /**         * after         * Encoder 생성자로 외부에서(Main.java)에서 객체를 주입해준다. - 의존성 주입.         * 따로 Encoder에서 UriEncoder를 생성하고, Base64Encoder를 생성하면서 불필요하게 작업할 필요가 없어져         * testable한 코드가 된다.         */        Encoder encoder = new Encoder(new Base64Encoder());        Encoder urlEncoder = new Encoder(new UrlEncoder());        String res = encoder.encode(url);        System.out.println(res);    }}