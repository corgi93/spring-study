package com.example.ioc.springaop.controller;import com.example.ioc.springaop.annotation.Decode;import com.example.ioc.springaop.annotation.Timer;import com.example.ioc.springaop.dto.User;import javafx.scene.paint.Stop;import org.springframework.util.StopWatch;import org.springframework.web.bind.annotation.*;@RestController@RequestMapping("/api")public class RestApiController {    @GetMapping("/get/{id}")    public String get(            @PathVariable Long id,            @RequestParam String name    ) {        return id + ", " + name;    }    @PostMapping("/post")    public User post(            @RequestBody User user    ) {        return user;    }    @Decode    @PutMapping("/put")    public User put(            @RequestBody User user    ) {        System.out.println("put");        System.out.println(user);        return user;    }    /**     * 커스텀 Annotation으로 반복적인 작업을 처리     */    @Timer    @DeleteMapping("/delete")    public void delete() throws InterruptedException {        // db logic        Thread.sleep(1000 * 2);    }}