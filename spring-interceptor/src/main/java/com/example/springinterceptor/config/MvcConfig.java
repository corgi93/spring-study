package com.example.springinterceptor.config;import com.example.springinterceptor.interceptor.AuthInterceptor;import lombok.RequiredArgsConstructor;import org.springframework.context.annotation.Configuration;import org.springframework.web.servlet.config.annotation.InterceptorRegistry;import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;@Configuration@RequiredArgsConstructorpublic class MvcConfig implements WebMvcConfigurer {    // RequiredArgsConstructor로 생성자로 주입    private final AuthInterceptor authInterceptor;    @Override    public void addInterceptors(InterceptorRegistry registry) {        // 원하는 주소를 addPathPatterns로 넣거나, 빼고자 하는 주고를 excludePathPatterns 으로 뺼 수 있다.        // 특정 Controller에 걸어주거나 특정 URL에 매칭해주는게 가장 좋음        registry.addInterceptor(authInterceptor).addPathPatterns("/api/private/*");    }}